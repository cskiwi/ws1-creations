<!doctype html>
<html>
<head>
    <title>{{ pageTitle }}</title>
    <style>
        {% block pageStyle %}
        div {
            padding-left: 1em;
            margin-left: 1em;
        }
        footer {
            text-align: center;
            padding-top: 2em; }
        {% endblock %}
    </style>
</head>
<body>
<h1>{{ pageTitle }}</h1>
<div>
    {% block pageContent %}
    {% endblock %}
</div>
{% block pageFooter %}
    <footer>This is the footer</footer>
{% endblock %}
</body>
</html>